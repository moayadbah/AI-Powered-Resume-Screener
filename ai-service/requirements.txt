# Pinned exactly. torch and sentence-transformers change behaviour between minor
# releases, and a floating version means scores that shift for no visible reason.
#
# Run `pip-audit -r requirements.txt` before bumping anything, and keep these
# current - an old pin here is a security finding, not a stability win.
fastapi==0.141.1
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
httpx==0.28.1
sentence-transformers==5.7.0
transformers==5.15.0
torch==2.13.0
numpy==2.3.4
