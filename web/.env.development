# Local dev defaults. Vite inlines VITE_ vars into the bundle - nothing secret here.
VITE_API_BASE_URL=http://localhost:8080

# Serve stubbed API responses from the contract examples, so the dashboard can
# be developed before api-service exists. Set to false to hit the real API.
VITE_USE_MOCKS=true
